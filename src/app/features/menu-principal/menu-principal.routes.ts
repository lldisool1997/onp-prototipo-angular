import { Routes } from '@angular/router';
import { MenuPrincipalPage } from './pages/menu-principal/menu-principal.page';

export const MENU_PRINCIPAL_ROUTES: Routes = [
  { path: '', component: MenuPrincipalPage }
];
